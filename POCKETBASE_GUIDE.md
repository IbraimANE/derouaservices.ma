# دليل تشغيل وربط PocketBase مع موقع خدمات الدروة

تم تجهيز وتكامل الموقع بالكامل للعمل مع خادم وقاعدة بيانات **PocketBase**، مع الحفاظ على التوافق مع وضع عدم الاتصال (Offline Fallback).

---

## 🚀 1. تحميل وتشغيل PocketBase

### أ) على حاسوبك الشخصي (Local Development):
1. قم بتحميل نسخة PocketBase المتوافقة مع نظامك (Windows / Linux / Mac) من الموقع الرسمي:
   👉 [https://pocketbase.io/docs/](https://pocketbase.io/docs/)
2. استخرج ملف `pocketbase.exe` في مجلد خاص (مثلاً: `C:\pocketbase`).
3. افتح موجه الأوامر (PowerShell أو CMD) في ذلك المجلد وشغّل:
   ```bash
   ./pocketbase serve
   ```
4. سيفتح الخادم على الرابط: `http://127.0.0.1:8090`
5. ادخل على لوحة التحكم للمرة الأولى: `http://127.0.0.1:8090/_/` وأنشئ حساب الأدمن الخاص بك.

---

## 📂 2. استيراد الجداول (Import Collections Schema)

لقد قمنا بتجهيز ملف `pocketbase_schema.json` في مجلد المشروع ليحتوي على جميع الحقول المطلوبة تلقائياً:

1. افتح لوحة تحكم PocketBase على المتصفح: `http://127.0.0.1:8090/_/`
2. اضغط على أيقونة **Settings** (الإعدادات) في الأسفل على اليسار.
3. اختر **Sync / Import collections**.
4. انسخ محتوى الملف `pocketbase_schema.json` والصقه، أو اضغط على استيراد، ثم اضغط **Review & Save**.
5. ستجد جدولين تم إنشاؤهما فوراً:
   - **`services`**: جدول الحرفيين والخدمات.
   - **`advertisements`**: جدول الإعلانات وطلبات الإشهار.

---

## 🔐 3. ضبط صلاحيات الوصول (API Rules)

في جداول PocketBase (`services` و `advertisements`)، تأكد من ضبط القواعد كالتالي لتسمح للزوار بالتسجيل وعرض البيانات:

| العملية (Action) | جدول `services` | جدول `advertisements` |
|------------------|----------------|----------------------|
| **List/Search** (العرض في الموقع) | اتركه فارغاً `""` (مفتوح للجميع) | اتركه فارغاً `""` (مفتوح للجميع) |
| **View** (عرض تفاصيل خدمة) | اتركه فارغاً `""` (مفتوح للجميع) | اتركه فارغاً `""` (مفتوح للجميع) |
| **Create** (تسجيل حرفي/إشهار جديد) | اتركه فارغاً `""` (مفتوح للجميع) | اتركه فارغاً `""` (مفتوح للجميع) |
| **Update** (تعديل وتوثيق) | للمدير فقط أو `status = 'approved'` | للمدير فقط |
| **Delete** (حذف خدمة) | للمدير فقط | للمدير فقط |

*(ملاحظة: ترك الحقل فارغاً في PocketBase يعني أنه متاح بدون قيود مصادقة API).*

---

## 🌐 4. ربط الموقع بعنوان خادم PocketBase

* في بيئة التطوير المحلية (Local): الموقع متصل افتراضياً بـ `http://127.0.0.1:8090`.
* عند رفع الموقع والخادم للإنترنت (Production):
  أنشئ ملف `.env` في المشروع وضع فيه رابط خادمك:
  ```env
  VITE_POCKETBASE_URL=https://pb.derouaservices.ma
  ```

---

## 💡 كيف يعمل النظام الآن؟
1. **تسجيل الحرفي**: عندما يدخل أي حرفي على الموقع ويضغط **"إضافة خدمة / حرفي"**، يُرسل طلبه فوراً إلى PocketBase بحالة `pending` وموثق `verified: false`.
2. **الموافقة والتوثيق**: من لوحة إدارة PocketBase أو من لوحة إدارة الموقع، يتم توثيق الحرفي وتظهر له العلامة الزرقاء المعتمدة.
3. **التشغيل بدون إنترنت**: إذا كان خادم PocketBase مغلقاً أو المستخدم غير متصل، يعمل الموقع بسلاسة تامة بالاعتماد على البيانات المحفوظة محلياً.
